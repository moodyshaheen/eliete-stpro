# 🔧 حل مشكلة 404 على GitHub Pages

## المشكلة:
بعد تحويل المشروع إلى Public، GitHub Pages يعطي خطأ 404.

## الحل:

### 1️⃣ تحقق من إعدادات GitHub Pages

1. اذهب إلى المستودع على GitHub: `https://github.com/moodyshaheen/eliete-stpro`
2. اضغط على **Settings** (في أعلى الصفحة)
3. اسكرول لأسفل إلى قسم **Pages** (في القائمة الجانبية)
4. في **Source**، تأكد من اختيار:
   - ✅ **GitHub Actions** (وليس "Deploy from a branch")
5. احفظ التغييرات

### 2️⃣ تشغيل GitHub Actions Workflow

#### أ. تحقق من أن Workflow موجود:
   - اذهب إلى تبويب **Actions** في المستودع
   - يجب أن ترى workflow اسمه "Deploy to GitHub Pages"

#### ب. تشغيل Workflow يدوياً:
   - اضغط على **Actions**
   - اختر workflow "Deploy to GitHub Pages"
   - اضغط **Run workflow** (في الجانب الأيمن)
   - اختر branch: **main**
   - اضغط **Run workflow**

#### ج. انتظر حتى ينتهي البناء:
   - سيستغرق 2-5 دقائق
   - ستظهر علامة ✅ خضراء عند النجاح
   - ستظهر علامة ❌ حمراء عند الفشل

### 3️⃣ إذا فشل البناء:

#### تحقق من الأخطاء:
   - اضغط على workflow الفاشل
   - اضغط على job "build"
   - اقرأ الأخطاء في الـ logs

#### الأخطاء الشائعة:

**خطأ: npm ci failed**
```bash
# الحل: تأكد من وجود package-lock.json في frontend و admin
```

**خطأ: Build failed**
```bash
# الحل: تأكد من أن جميع dependencies موجودة
```

**خطأ: No such file or directory**
```bash
# الحل: تأكد من أن المسارات صحيحة في workflow
```

### 4️⃣ بعد نجاح البناء:

1. انتظر 1-2 دقيقة
2. اذهب إلى: `https://moodyshaheen.github.io/eliete-stpro/`
3. يجب أن يعمل الموقع الآن! ✅

---

## 🔄 إذا لم يعمل بعد:

### الحل البديل: رفع ملفات يدوياً

1. **بناء المشروع محلياً:**
```bash
cd frontend
npm install
npm run build
```

2. **إنشاء branch gh-pages:**
```bash
git checkout -b gh-pages
```

3. **نسخ ملفات dist:**
```bash
# انسخ محتويات frontend/dist إلى الجذر
cp -r frontend/dist/* .
```

4. **رفع التغييرات:**
```bash
git add .
git commit -m "Deploy to GitHub Pages"
git push origin gh-pages
```

5. **تغيير إعدادات GitHub Pages:**
   - Settings → Pages
   - Source: اختر **gh-pages branch**
   - Folder: **/ (root)**

---

## ✅ التحقق من أن كل شيء صحيح:

- [ ] المشروع Public على GitHub
- [ ] GitHub Pages مفعل في Settings
- [ ] Source = "GitHub Actions"
- [ ] Workflow نجح (علامة ✅ خضراء)
- [ ] انتظرت 1-2 دقيقة بعد النشر
- [ ] جربت الرابط: `https://moodyshaheen.github.io/eliete-stpro/`

---

## 🆘 إذا استمرت المشكلة:

1. **تحقق من الـ logs في Actions**
2. **تأكد من أن `.nojekyll` موجود في الجذر**
3. **تأكد من أن `index.html` موجود في `frontend/dist` بعد البناء**

